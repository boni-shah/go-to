import { createAction } from "@ngrx/store";

export const sidenavClick = createAction("[Sidenav] Click");
export const sidenavSave = createAction("[Sidenav] Save");
export const sidenavAutoSave = createAction("[Sidenav] AutoSave");
export const sidenavSearch = createAction("[Sidenav] Search");